<h2><%= link_to "#{project.name}", project_path(project.id), class: "link" %></h2>
<%= link_to "Edit ", edit_project_path(project), class: "link" %>
<%= link_to "Delete", project_path(project), method: "delete", confirm: "Are you sure?", class: "link" %>